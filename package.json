{
  "name": "kucoin-activities-telegram-monitor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "collect": "node scripts/collect.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
