# Оптимизация расписания KuCoin Activities Monitor

## Что было изменено:
- Расписание cron скорректировано с `*/15 * * * *` (каждые 15 мин = 96 проверок/сутки) на `20 */2 * * *` (каждые 2 часа = 12 проверок/сутки).

## Расчет потребления:
- **Частота**: 12 проверок в сутки = ~360 запусков в месяц.
- **Длительность 1 проверки**: ~18 секунд (округляется GitHub до 1 минуты).
- **Итоговый расход**: **360 Action minutes / месяц**.
